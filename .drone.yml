kind: pipeline
type: docker
name: build

trigger:
  event:
    - tag

steps:
  - name: greeting
    image: node:14.15.0-alpine
    commands:
      - docker build -t docker.pkg.github.com/codermango/${DRONE_REPO}/${DRONE_REPO}-image:${DRONE_TAG} .
